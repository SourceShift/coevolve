# EPIC-10 — Docs + packaging
Status: todo · Depends: 09
## Goal
Make it installable + extensible by others.
## Deliverables
- coevolve/README + extend-guide: add an MCP tool / a new integration (register
  against the ABCs), add a dashboard. omnigent attribution (Apache-2.0).
- Package (pyproject console-script) + a demo recipe for the deck/DocSend.
## Acceptance
- Fresh checkout: `pip install -e . && coevolve` launches opencode with the tools.
